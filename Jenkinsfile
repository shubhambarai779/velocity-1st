pipeline {
    agent {
        node {
            label 'built-in'
            customWorkspace '/mnt/jenkins77'
        }
    }
	stages {
        stage('new_folder') {
            steps {
                sh 'mkdir Dee'
            }
        }
		
    }
}
